import { CustomEllipsisDirective } from './custom-ellipsis.directive';

describe('CustomEllipsisDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomEllipsisDirective();
    expect(directive).toBeTruthy();
  });
});
